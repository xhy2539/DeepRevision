简单
(2分)
在操作系统中，当一个进程执行系统调用时，其执行环境从用户空间切换至内核空间，这一切换的根本原因是
A
A 用户空间无法直接访问硬件设备控制器
B
B 内核空间拥有更高优先级的CPU时间片分配权
C
C 所有文件系统操作必须在内核态下由专用线程完成
D
D 用户空间代码段被操作系统强制标记为只读以防止篡改
2
中等
(2分)
某进程在运行过程中请求一个已被其他进程占用的资源，且该资源类型仅有一个实例，则操作系统可能检测到死锁的充分条件是
A
A 资源分配图中存在从该进程出发的请求边
B
B 该进程处于阻塞队列且等待时间超过系统设定阈值
C
C 资源分配图中存在包含该进程的环路
D
D 系统空闲内存页帧数低于进程请求的最小物理页数
3
中等
(2分)
内核空间与用户空间的严格隔离主要依靠硬件机制实现，该机制的核心功能是
A
A 在进程切换时自动刷新TLB以防止地址混淆
B
B 根据当前特权级禁止用户态代码执行敏感指令
C
C 将用户空间虚拟地址映射到独立的物理内存区域
D
D 为每个用户进程分配唯一的内核栈副本
4
简单
(2分)
设备驱动程序在操作系统中的核心作用是
A
A 将抽象的I/O请求转换为特定设备控制器可识别的硬件指令序列
B
B 替代文件系统模块直接管理磁盘块的分配与回收
C
C 在用户空间为应用程序提供统一的图形界面API
D
D 作为进程调度器的子模块动态调整设备中断的响应优先级
5
简单
(2分)
内存管理子系统在内核中承担的关键职责不包括
A
A 为每个进程建立并维护页表结构以支持虚拟内存
B
B 响应缺页异常并从磁盘加载所需页面到物理内存
C
C 直接处理来自键盘控制器的扫描码中断并转换为字符事件
D
D 实施页面置换算法以决定淘汰哪个物理页帧
6
中等
(2分)
文件系统子系统在内核中实现的层次结构中，逻辑文件系统模块的主要任务是
A
A 将open()系统调用参数解析为inode号并验证访问权限
B
B 将文件逻辑块号映射为磁盘上的物理柱面/磁头/扇区地址
C
C 向设备驱动发送具体的读写命令并等待硬件完成中断
D
D 管理FAT或inode表的磁盘块分配位图
7
简单
(2分)
当进程因请求资源而被阻塞时，其状态从运行态转入阻塞态，此时该进程被移入的队列是
A
A 就绪队列
B
B 阻塞队列
C
C 就绪挂起队列
D
D 阻塞挂起队列
8
中等
(2分)
以下关于系统调用接口的描述，正确的是
A
A 系统调用接口是用户程序与内核之间唯一的受控通信通道
B
B 每个系统调用都对应一个独立的内核线程来并发执行
C
C 用户程序可通过直接跳转到内核代码地址的方式绕过系统调用接口
D
D 系统调用接口仅在分时系统中存在，在实时系统中被硬件中断直接替代
9
困难
(2分)
在模块化设计的操作系统中，设备驱动模块与内存管理模块之间的耦合关系体现为
A
A 设备驱动在执行DMA操作前必须向内存管理模块申请连续的物理页帧
B
B 内存管理模块定期扫描设备驱动代码段以回收未使用的函数页
C
C 设备驱动直接修改内存管理模块的页表项以加速I/O数据拷贝
D
D 内存管理模块为每个设备驱动分配独立的虚拟地址空间
10
困难
(2分)
内核空间中，文件系统、内存管理、设备驱动、进程调度四大子系统共同构成操作系统核心功能，它们之间最典型的非完全分层特征表现为
A
A 进程调度器在选择下一个运行进程时，需查询内存管理模块获取该进程的页表基址寄存器值
B
B 文件系统在写入数据时直接调用设备驱动的read()函数完成底层传输
C
C 设备驱动在初始化时必须向进程调度器注册一个专用内核线程
D
D 内存管理模块为每个文件系统缓存分配固定大小的SLAB内存池

填空题（5题）
▲ 点击折叠
11
中等
(2分)
在资源分配图中，若存在一个由进程和资源节点构成的环路，且每种资源类型仅有单个实例，则该环路表明系统中发生了（）。
12
简单
(2分)
操作系统内核通过为每个进程分配唯一的（）来标识其身份，并以此实现对进程生命周期、资源归属及调度状态的精确管理。
13
中等
(2分)
在资源分配图中，若存在一个由进程和单实例资源构成的循环，例如 T → D → U → C → T，则表明系统已发生（ ）。
14
中等
(2分)
操作系统内核通过（ ）机制将用户空间对内存的逻辑请求转换为对物理内存的实际访问，并支持按需分页与页面置换。
15
中等
(2分)
在操作系统中，用户程序运行于用户空间，而系统核心功能（如进程调度、内存管理、文件系统、终端驱动）运行于（______），该空间具有直接访问硬件和内核数据结构的权限，且不受用户程序非法干扰。

判断题（5题）
▲ 点击折叠
16
简单
(2分)
内核是操作系统的核心部分，运行于内核空间，直接管理硬件资源并为上层提供系统调用接口。
正正确
错错误
17
中等
(2分)
当一个进程发出对某资源的请求时，该请求在资源分配图中表现为从该进程节点指向对应资源节点的有向边。
正正确
错错误
18
中等
(2分)
设备驱动程序运行在内核空间，直接与硬件设备控制器通信，并为上层提供统一的字符I/O或块I/O接口。
正正确
错错误
19
中等
(2分)
系统调用是用户空间进程主动发起的陷入内核的唯一合法机制，其执行过程必然导致CPU从用户态切换到内核态。
正正确
错错误
20
简单
(2分)
文件系统是操作系统内核的一个核心子系统，负责管理存储设备上的文件组织、目录结构、访问权限及数据持久化。
正正确
错错误

简答题（3题）
▲ 点击折叠
21
中等
(20分)
请完整说明进程在操作系统中的定义，并结合进程状态转换图，解释当一个进程请求某资源但该资源当前不可用时，其状态如何变化；该状态变化涉及哪两个核心队列？
22
中等
(20分)
在资源分配图中，若存在一条从进程T出发、经资源D、进程U、资源C再回到进程T的环路（即T→D→U→C→T），且每种资源类型仅有一个实例，请回答：（1）该环路是否表明系统已发生死锁？为什么？（2）请明确指出图中哪条边表示‘请求资源’，哪条边表示‘分配资源’；（3）若进程T当前持有资源C并请求资源D，而进程U持有资源D并请求资源C，请说明此时两个进程所处的状态及其根本原因。
23
中等
(20分)
内核作为操作系统的核心组件，其主要功能包括硬件抽象、模块化设计与资源管理。请结合具体子系统，回答以下问题：（1）内核如何通过硬件抽象实现对底层设备差异的屏蔽？（2）内存管理与设备驱动这两个子系统在内核中分别承担什么职责？（3）当用户进程发起I/O请求时，该请求如何经由内核完成资源分配与设备操作？